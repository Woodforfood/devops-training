---
- hosts: "tag_InstanceSt_zombie"
  connection: "local"
  gather_facts: false
  vars:
    region: "eu-central-1"

  tasks:  
  - name: "Terminate tagged EC2 instances"
    ec2:
      state: "absent"
      region: "{{ region }}"
      instance_ids: "{{ ec2_id }}"
      wait: true
    delegate_to: "localhost"
